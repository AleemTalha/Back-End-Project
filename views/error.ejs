<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuyFinity - error</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <style>
        :root {
            --black: #000000;
            --brown: #48a6a7;
            --yellow: #ffb22c;
            --white: #f7f7f7;
            --dWhite: #f2efe7;
        }
        .error-box {
            background-color: var(--dWhite);
            color: var(--brown);
            padding: 15px;
            border: 2px solid var(--brown);
            border-radius: 5px;
            width: 300px;
            text-align: center;
            font-family: Arial, sans-serif;
            margin: 20px auto;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
        }
        .error-code {
            font-size: 20px;
            font-weight: bold;
        }
        .error-message {
            font-size: 16px;
        }
        * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      .lora {
        font-family: "Lora", serif;
        font-weight: 500;
      }
      .great {
        font-family: "Great Vibes", serif;
        font-weight: 400;
      }
    </style>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Lora:ital,wght@0,400..700;1,400..700&display=swap"
      rel="stylesheet"
    />
</head>
<body>
    <div class="navbar fixed-top border-bottom shadow-lg great" style="background-color: var(--dWhite);">
        <div class="great display-4 px-lg-5 px-md-4 px-2"><a href="/shop">BuyFinity</a></div>
      </div>
    <div class="error-box lora" style="margin-top:30vh;">
        <div class="error-code"><% if (title && title.length > 0) { %><%= title %><% } %></div>
        <div class="error-message"><% if (message && message.length > 0) { %><%= message %><% } %></div>
    </div>
</body>
</html>
